<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="RoundedButton">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="RoundedEditText">
        <item name="android:background">@drawable/rounded_edittext</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>
    <style name="ToolbarTitleStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">18sp</item>  <!-- Размер текста -->
        <item name="android:textStyle">bold</item>  <!-- Жирный шрифт -->
        <item name="android:textColor">@android:color/black</item>  <!-- Черный цвет -->
    </style>


</resources>
